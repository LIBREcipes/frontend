import { EllipsisJoinPipe } from './ellipsis-join.pipe';

describe('EllipsisJoinPipe', () => {
  it('create an instance', () => {
    const pipe = new EllipsisJoinPipe();
    expect(pipe).toBeTruthy();
  });
});
